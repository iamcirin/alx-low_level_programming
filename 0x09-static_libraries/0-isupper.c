#include "main.h"

/**
 * _isupper - Entry point
 *
 * @c: param character
 *
 * Return: returns 1 is maj and 0 if min
 */

int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
