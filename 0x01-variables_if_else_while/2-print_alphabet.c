#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: checks if the last digit of n
 *
 * Return: returns 0 (Success)
 */
int main(void)
{
	char letter = 'a';
	/* your code goes there */
	while (letter <= 'z')
	{
		putchar(letter);
		putchar('\n');
		letter++;
	}
	return (0);
}
