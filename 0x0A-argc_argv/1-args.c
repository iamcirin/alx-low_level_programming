#include <stdio.h>

/**
 * main - Entry point
 * @argc: param
 * @argv: param
 * Return: returns success
 */

int main(int argc, __attribute__((unused)) char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
