#include "main.h"

/**
 * main - a program that prints its name, followed by a new line.
 * @argc: returns the number of elements passed to the CLI
 * @argv: an array of pointers to char of the element passed
 * Return: returns 0 - success
 */

int main(int __attribute__((unused)) argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);
	return (0);
}

