#include<stdio.h>

/**
 * main - an Entry point
 * Description: a program that prints the name of the current file
 * Return: returns 0
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
