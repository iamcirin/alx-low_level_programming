#include <stdio.h>

/**
 * main - Entry point
 * Description: a function that prints the file name
 * compiled from with a new line
 *
 * Return: return a 0
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
